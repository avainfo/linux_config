#!/usr/bin/env bash
set -euo pipefail
if [[ $# -lt 1 ]]; then
    echo "Usage: trace-process <command...>"
    exit 1
fi
mkdir -p "./traces"
LOG_FILE="./traces/strace_$(date +%Y%m%d_%H%M%S).log"
echo "Tracing to $LOG_FILE"
strace -ff -T -tt -o "$LOG_FILE" "$@"
